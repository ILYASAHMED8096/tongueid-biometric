[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tongueid-biometric"
version = "0.1.0"
description = "TongueID biometric prototype (feature engineering + ML)"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["tongueid"]
